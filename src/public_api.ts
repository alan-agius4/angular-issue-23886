import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';
import { PrimaryAngularComponent } from './primary.component';

@NgModule({
    imports: [
        RouterModule
    ],
    declarations: [
        PrimaryAngularComponent
    ]
})
export class LibModule { }